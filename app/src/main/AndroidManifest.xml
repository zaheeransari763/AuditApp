<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.auditapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CalculateExpenseActivity"></activity>
        <activity android:name=".CheckBalanceActivity" />
        <activity android:name=".RegisterEmailActivity" />
        <activity android:name=".LoginEmailActivity" />
        <activity android:name=".ExpenseActivity" />
        <activity android:name=".GoogleActivity" />
        <activity android:name=".PhonePayActivity" />
        <activity android:name=".BankActivity" />
        <activity android:name=".PaytmActivity" />
        <activity android:name=".SavingsActivity" />
        <activity android:name=".Profile_upload" />
        <activity android:name=".Dashboard" />
        <activity android:name=".ProfileReg" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>